<odoo>
  <data noupdate="1">

    <!-- Rol para plantilla ACME (1 recolector) -->
    <record id="rol_recolector_plantilla_acme" model="malkion.plantilla_rol">
      <field name="plantilla_id" ref="plantilla_acme"/>
      <field name="rol_id" ref="job_recolector"/>
      <field name="cantidad">1</field>
    </record>

    <!-- Rol para plantilla BioTec (1 cazador) -->
    <record id="rol_cazador_plantilla_biotec" model="malkion.plantilla_rol">
      <field name="plantilla_id" ref="plantilla_biotec"/>
      <field name="rol_id" ref="job_cazador"/>
      <field name="cantidad">1</field>
    </record>

  </data>
</odoo>
