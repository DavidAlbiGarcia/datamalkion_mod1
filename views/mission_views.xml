<odoo>
    <!-- Vista de lista de misiones -->
    <record id="view_malkion_mission_tree" model="ir.ui.view">
        <field name="name">malkion.mission.tree</field>
        <field name="model">malkion.mission</field>
        <field name="arch" type="xml">
            <tree string="Misiones">
                <field name="name" string="Nombre de la Misión"/>
                <field name="estado" string="Estado"/>
                <field name="jefe_data_id" string="Jefe de Datos"/>
                <field name="gestor_hunters_id" string="Gestor de Hunters"/>
                <field name="gestor_equipo_id" string="Gestor de Equipo"/>
                <field name="gestor_transportes_id" string="Gestor de Empleados"/>
                <field name="observaciones" string="Observaciones"/>
            </tree>
        </field>
    </record>

    <!-- Vista de formulario para misiones -->
    <record id="view_malkion_mission_form" model="ir.ui.view">
        <field name="name">malkion.mission.form</field>
        <field name="model">malkion.mission</field>
        <field name="arch" type="xml">
            <form string="Misión">
                <sheet>
                    <group>
                        <!-- Campo invisible para 'empleados_equipo_ids' -->
                        <field name="empleados_equipo_ids" invisible="1"/>
                        <!-- Campo invisible para 'empleados_transporte_ids' -->
                        <field name="empleados_transporte_ids" invisible="1"/>

                        <field name="name" string="Nombre de la Misión" readonly="1" groups="malkion.group_AGENTESCAMPO"/>

                        <field name="estado" string="Estado"/>

                        <field name="jefe_data_id" string="Jefe de Datos" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                        <field name="gestor_hunters_id" string="Gestor de Hunters" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                        <field name="gestor_equipo_id" string="Gestor de Equipo" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                        <field name="gestor_transportes_id" string="Gestor de Empleados" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                        <field name="observaciones" string="Observaciones"
                            readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                    </group>
                    <group>
                        <field name="puntos_interes_ids" widget="many2many_tags" string="Puntos de Interés" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                        <field name="roles_ids" widget="many2many_tags" string="Roles" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                        <field name="equipo_ids" widget="many2many_tags" string="Equipo Necesario" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                        <field name="transporte_ids" widget="many2many_tags" string="Transporte Necesario" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                    </group>
                    <group>
                        <!-- Responsables de equipo -->
                        <field name="responsable_equipo_id" string="Responsables de Equipo" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                        
                        <!-- Responsables de transporte -->
                        <field name="responsable_transporte_id" string="Responsables de Transporte" readonly="1" groups="malkion.group_AGENTESCAMPO"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción para misiones -->
    <record id="action_malkion_mission" model="ir.actions.act_window">
        <field name="name">Misiones</field>
        <field name="res_model">malkion.mission</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <!-- Menú para misiones -->
    <menuitem id="menu_malkion_mission" name="Misiones" parent="menu_malkion_root" sequence="10"/>
    <menuitem id="menu_mission_list" name="Ver Misiones" parent="menu_malkion_mission" action="action_malkion_mission" sequence="1"/>
</odoo>
